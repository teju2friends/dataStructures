package recurssion;

import org.junit.Test;

public class NbitStringTest {
    @Test
    public void gen() {
        NbitString.generate(0);
        NbitString.generate(1);
        NbitString.generate(2);
        NbitString.generate(3);
        NbitString.generate(4);
    }
}